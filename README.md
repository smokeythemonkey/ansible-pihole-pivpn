# ansible-pihole-pivpn

**WIP**
ansible playbook that setups pihole and pivpn for access to network from behind DSLITE NAT

should work in conjunction with a external server setup as a wireguard server
